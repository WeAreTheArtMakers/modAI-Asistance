name: Release Desktop Builds

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to create or update"
        required: true
        default: "v0.1.4"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            label: linux
            command: npm run build:portable
            artifact_path: dist/modAI-tauri-linux-*.tar.gz
          - os: windows-latest
            label: windows
            command: npm run build:portable
            artifact_path: dist/modAI-tauri-windows-*.zip
          - os: macos-15
            label: macos-arm64
            command: npm run build:mac:arm64
            rust_target: aarch64-apple-darwin
            artifact_path: dist/modAI-tauri-macos-arm64.zip
          - os: macos-15-intel
            label: macos-x64
            command: npm run build:mac:x64
            rust_target: x86_64-apple-darwin
            artifact_path: dist/modAI-tauri-macos-x64.zip
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target || '' }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Run tests
        run: npm test

      - name: Build release artifact
        run: ${{ matrix.command }}
        env:
          MODAI_CARGO_NET_OFFLINE: "false"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-assets-${{ matrix.label }}
          path: ${{ matrix.artifact_path }}

  release:
    name: Publish Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          pattern: release-assets-*
          merge-multiple: true

      - name: Create or update GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          files: release-assets/*
          generate_release_notes: true
